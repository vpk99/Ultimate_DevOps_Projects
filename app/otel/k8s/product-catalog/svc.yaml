apiVersion: v1
kind: Service

metadata:
  name: product-catalog-svc
  labels:
    app: product-catalog
  namespace: project

spec:
  type: ClusterIP
  
  selector:
    app: product-catalog
  ports:
    - port: 8088
      targetPort: 8088
      protocol: TCP